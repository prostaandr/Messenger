<UserControl x:Class="Messenger.Client.WPF.Views.ChatPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Messenger.Client.WPF.Views"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Background="{StaticResource backgroundColor}"
      Foreground="{StaticResource mainFontColor}"
             Loaded="UserControl_Loaded">

    <Grid>
        <StackPanel>
            <TextBlock x:Name="testTextBloc">test</TextBlock>
        <TextBox x:Name="testTextBox" Height="20" Width="100" HorizontalAlignment="Left"></TextBox>
        <Button Height="30" Width="100" HorizontalAlignment="Left" Click="Button_Click"></Button>
        </StackPanel>
    </Grid>
</UserControl>
